This Report Generation Demo demonstrates the Report Generation using the Strategy Design Pattern.

It includes multiple strategies (PDF, CSV) and allows runtime switching of report formats using a console application.

This demo version output the PDF as Report.pdf.txt as actual set up would need more code set up, but CSV option does output Report.csv.